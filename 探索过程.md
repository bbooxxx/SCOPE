# SCOPE 探索过程

## 1. 输入与仓库审计

- 原项目已从 `sparsh0mittal/destiny_3d_cache` 的用户 fork 拉取到 `Simulation/SCOPE`，保留 DESTINY C++ 阵列搜索与 Makefile 构建链。
- 附件 TDMSim 用于确认 2T0C eDRAM 的行相关读路径、保持时间和 hybrid/pure 路径表达；它没有被当作指令，也没有照搬其电路级仿真器。
- 用户最后提供的两张 Device Library 截图成为器件表的唯一输入，不再访问飞书。截图的 7 列、10 类指标均转录到 `config/device_library.json`。
- 旧 DESTINY 只建单层，且 cell 参数无法直接表达截图全部指标；因此新增 `scope.py` 负责三级组合，而不是大改原 C++。

## 2. 参数探索与关键取舍

| 未确定项 | 采用值 | 依据与推导 |
|---|---:|---|
| L1–L2 / L2–L3 延迟 | 2 ns/跳 | gem5 L2XBar 的 frontend=1、response=1，在 1 GHz 下为 2 cycles：[源码](https://gem5.googlesource.com/public/gem5/+/392c1ced53827198652f5eda58e1874246b024f4/src/mem/XBar.py) |
| crossbar 能耗 | 0.201 pJ/bit | DSENT Table II：2.06 mW / (1 GHz×0.16×64 bit)：[论文](https://projects.csail.mit.edu/wiki/pub/LSPgroup/PublicationList/dsent_nocs12.pdf) |
| 每跳事务能耗 | 0.115776 nJ | 0.201 pJ/bit×(512-bit line+64-bit request) |
| 片外延迟 | 45.812 ns | DDR4-2400 行冲突：tRP+tRCD+tCL+tBURST=14.16×3+3.332：[gem5 配置](https://gem5.googlesource.com/public/gem5/%2B/1d03f6de941520860c673b5f7954c82a46e8b191/src/mem/DRAMInterface.py) |
| 片外能耗 | 10.24 nJ/64 B | Horowitz 给出 DRAM I/O 超过约 20 pJ/bit，按 512 bit 换算：[ISSCC 2014](https://gwern.net/doc/cs/hardware/2014-horowitz-2.pdf) |
| 请求频率 | 50 M/s | SPEC CPU2017 的 L1 MPKI 范围 5.8–118.3；以约 50 MPKI、1 GIPS 作为记忆密集型演示：[PLOS ONE](https://doi.org/10.1371/journal.pone.0220135) |
| 读比例 | 0.75 | 无统一经典值，作为显式可配置 demo 假设 |
| 命中率 | 合成测量 | 8 MiB 工作集、Zipf α=1.1、20k 预热、50k 采样；不宣称是精确 workload trace |

器件动态指标的第一次实现把新表作为 DESTINY 结果的下限；完整运行后发现旧 DESTINY 总漏电会完全掩盖新表。源码审计显示输出可拆成 data-array 与 tag-array，但 data-array 内无法再可靠拆分 cell/periphery。因此最终规则改为“数据阵列漏电用新表替换、标签阵列漏电保留”，并在 JSON 同时保存 raw/effective 值，避免不可见修正。

eDRAM 表中的 `Lr(Nrow)`、`Er(Nrow)` 没有给闭式系数，因此用同容量、相联度、行宽的 eDRAM DESTINY 实例求值；2D/OSFET 选择截图的 hybrid 路径。固定 fJ 值按每 bit 解释并换算到 64 B；SRAM 的 27.5 pW 和 eDRAM 的约 100 aW 也按每数据 bit 解释。这是使表格能够代入系统评估所需的最小、明确假设。

## 3. 实现、试验与结果

实现包含：三级条件命中公式、独立层配置、SRAM/MRAM/eDRAM family 校验、LRU/FIFO/RANDOM、合成/固定/trace 命中率、WB+WA、后台脏行写回能耗、逐指令路径、BER/寿命/刷新约束、FoM 以及 `--explore` 组合选优。为证明层级不绑定器件，补齐了 9 个 `scope_l{1,2,3}_{family}.cfg`。

验证过程：

```text
make -j4                       通过；仅原项目 2 条 unused-variable warning
python3 -m py_compile ...      通过
python3 -m json.tool ...       通过
make test-scope                4/4 通过
完整运行 3 个 DESTINY 实例      通过
```

四个单元测试分别检查：7 个截图器件/字段完整性、SRAM 漏电换算、用户给定平均公式、WB 能耗计入但不增加关键延迟。

最终示例条件命中率为 `0.601260 / 0.526208 / 0.097290`；平均延迟 `13.036440 ns`。动态功耗 `97.519193 mW`（其中片外需求访问 `87.316480 mW`、WB 到 L2/L3 共 `1.878545 mW`），静态功耗 `298.937209 mW`，刷新 `4.194304e-7 mW`，总功耗 `396.456403 mW`，FoM `1.934842e-4 1/(ns·mW)`，全部约束通过。

随后实际运行 `--explore`：每层均遍历 SRAM/SOT-MRAM/2D-eDRAM，27/27 个三级组合完成且可行。当前最高 FoM 是 SRAM/SOT-MRAM/SOT-MRAM，对应 `12.765098 ns`、`216.892851 mW`、`3.611858e-4 1/(ns·mW)`。因此“每层器件可配置”不仅是 JSON 接口，也经过了全组合执行验证。

主要限制已保留而未伪造：无精确 trace、无排队/争用延迟、填充默认非关键路径、截图未给 BER、OSFET 的 N(BTI) 需外部标定、eDRAM pure path 需额外函数、数据阵列外围漏电无法从旧 DESTINY 输出中单独恢复。这个边界符合“简单行为级 demo”，也为以后替换更精确测量留出了 JSON 字段。
