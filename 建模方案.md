# SCOPE 三级异构缓存建模方案

## 1. 目标与边界

SCOPE 是一个行为级三级片上缓存 demo：计算单元发出 `load/store` 后，模型依次经过 L1、L1–L2 crossbar、L2、L2–L3 crossbar、L3，必要时访问片外 DRAM。L1/L2/L3 **均不绑定器件**，每层都可独立选择 SRAM、STT/SOT-MRAM 或 Si/TFET/2D/OSFET-eDRAM，并独立配置容量、相联度、行宽、替换策略、bank 数、BER、刷新周期和寿命参数。当前示例的 `L1=SRAM、L2=2D-eDRAM、L3=SOT-MRAM` 只是一组演示映射。

每一层启动一个独立、未改内部阵列搜索算法的 DESTINY 实例，使用其 bank、阵列、标签和外围结构结果；`scope.py` 在其上完成三级组合、命中率估计、WB+WA 行为、器件库替换、端到端延迟/能耗/功耗以及约束检查。不需要精确访问 trace：默认用带预热的 Zipf 合成地址流估计条件命中率，也可改成固定命中率或 JSONL trace。

附件 [TDMSim 论文](</Users/b0/Desktop/Desktop/Learning/Research/经历/26暑假 DATE/Paper/前期学习/eDRAM/2026-ISCA-TDMSim.pdf>) 中以行数、保持时间和混合/纯器件路径描述 2T0C eDRAM；SCOPE 保留同样的 `Nrow`、刷新—保持约束及 hybrid/pure 字段，但把它简化为可运行的系统级黑盒，不复现论文的电路级仿真。

## 2. 参数、行为与公式

**Device Library。** `config/device_library.json` 逐项转录了用户截图，替代旧 DESTINY 的器件指标；`fJ` 与 `pW/aW` 按单个数据位解释，`>10^15` 按保守下界 `10^15` 评估。表中没有给出的 BER 仍由层配置提供。

| 器件 | 读延迟 / 能耗 | 写延迟 / 能耗 | 漏电；刷新 | 寿命 | variation；密度；M3D |
|---|---|---|---|---|---|
| STT-MRAM | 10 ns；41 fJ | ~20 ns；100 fJ | 0；0 | 10^9 | low；53 F²；未定 |
| SOT-MRAM | ~1 ns；18.3 fJ | ~10 ns；102 fJ | 0；0 | 10^14 | low；100 F²；未定 |
| SRAM | ~1 ns；21.5 fJ | ~1 ns；16.2 fJ | 27.5 pW；0 | >10^15 | low；140 F²；N/A |
| Si-eDRAM | Lr,s(Nrow)；Er(Nrow) | 2 ns；~2.5 fJ | ½CΔV²；Pref(Nrow)@10 µs | >10^15 | low；~35 F² (XL)；N/A |
| TFET-eDRAM | Lr,s(Nrow)；Er(Nrow) | 3 ns；~10 fJ | Pref；~100 aW | >10^15 | low；~35 F² (XL)；N/A |
| 2D-eDRAM | hybrid Lr,s / pure Lr,d；Er | 20 ns；~10 fJ | Pref；~100 aW | >10^15 | low；~35 F² planar；1-tier 2TOC |
| OSFET-eDRAM | hybrid Lr,s / pure Lr,o；Er | ~10 ns；~10 fJ | Pref；~100 aW | N(BTI) | high BTI；~35/N F²；4-tier 2TOC |

固定器件值先换算到一条 64 B cache line，例如 `Edevice=efJ/bit×512/10^6 nJ`；有效读写延迟和动态能耗取 `max(DESTINY 层级结果, Device Library 数据位下限)`，防止旧 cell 参数覆盖新表，也避免把两套完整阵列能耗重复相加。eDRAM 的 `Lr(Nrow)`、`Er(Nrow)` 直接由相同容量/相联度的 DESTINY eDRAM 实例在 `Nrow=capacity/(banks×line_bytes)` 下求值；2D/OSFET 默认选 hybrid 路径。数据阵列漏电由新表替换，DESTINY 标签阵列漏电保留；刷新、寿命、variation、密度和 M3D 直接来自新表。OSFET 的寿命只有 `N(BTI)`，使用时必须另给 `bti_endurance_writes_per_line`。

**行为。** 缓存采用 write-back + write-allocate 和可选 LRU/FIFO/RANDOM。load/store 逐层探测；命中即停止，未命中则向下访问并向上填充；脏行写回能耗计入 `Eavg`，但默认视为有缓冲、非关键路径。gem5 的经典缓存说明支持 set-associative、LRU 类替换及层间 crossbar 路由，也说明常规 cached memory 为 write-back、read/write allocate：[classic caches](https://www.gem5.org/documentation/general_docs/memory_system/classic_caches/)。

**端到端公式。** `Ri` 是到达 Li 后的条件命中率，`r` 是读比例：

```text
Laccess,i = r·Lread,i + (1-r)·Lwrite,i + Lperipheral,i
Lavg = Laccess,1
     + (1-R1)(Lx12 + Laccess,2)
     + (1-R1)(1-R2)(Lx23 + Laccess,3)
     + (1-R1)(1-R2)(1-R3)Loff

Eavg = 同样的到达概率加权动态能耗 + 后台脏行写回能耗
Pdyn[mW] = λmem[s⁻¹] · Eavg[nJ] · 10⁻⁶
Pavg = Pdyn + ΣPleak,i + ΣPref,i
FoM = 1 / (Lavg · Pavg)
```

约束为 `BERi≤BERmax`、寿命内每行写次数不超过 endurance、eDRAM 的 `refresh_interval≤retention_time`。密度和 M3D 进入报告；high variation 默认使方案不可行，只有显式设置 `allow_high_variation=true` 才放行，因此 OSFET 的 high-BTI 不会被静默忽略。

**经典缺省值。** crossbar 采用 1 GHz 下请求/响应各 1 cycle，即 2 ns，来自 gem5 `L2XBar` 的 256-bit、frontend=1、response=1 配置：[gem5 XBar.py](https://gem5.googlesource.com/public/gem5/+/392c1ced53827198652f5eda58e1874246b024f4/src/mem/XBar.py)。DSENT 的 45 nm、64-bit、1 GHz、注入率 0.16 crossbar 功率为 2.06 mW，换算得 `0.201 pJ/bit`，一次 512-bit 数据加 64-bit 请求为 `0.115776 nJ`：[DSENT Table II](https://projects.csail.mit.edu/wiki/pub/LSPgroup/PublicationList/dsent_nocs12.pdf)。片外采用 DDR4-2400 行冲突关键路径 `tRP+tRCD+tCL+tBURST=45.812 ns`：[gem5 DDR4-2400](https://gem5.googlesource.com/public/gem5/%2B/1d03f6de941520860c673b5f7954c82a46e8b191/src/mem/DRAMInterface.py)；能耗按约 `20 pJ/bit×512=10.24 nJ`：[Horowitz ISSCC 2014](https://gwern.net/doc/cs/hardware/2014-horowitz-2.pdf)。`λmem=50 M/s` 是 1 GIPS、约 50 MPKI 的记忆密集型演示点；SPEC CPU2017 实测 L1 MPKI 从 5.8 到 118.3，505.mcf_r 为 65.8、519.lbm_r 为 50.9，因此该值只作可配置基线：[PLOS ONE Table 3](https://doi.org/10.1371/journal.pone.0220135)。

## 3. 运行与示例结果

在仓库根目录运行：

```bash
make -j4
make test-scope
python3 scope.py config/scope_example.json --json-output results/scope_example.json
```

改器件时同时选择同层对应 family 的 DESTINY 配置：`scope_l{1,2,3}_{sram,edram,mram}.cfg`；同一 family 内只改 `device` 即可，例如 `STT-MRAM↔SOT-MRAM` 或四种 eDRAM。每层 `candidates` 展示了 SRAM/SOT-MRAM/2D-eDRAM 三类选择，`--explore` 会组合并按满足约束后的最高 FoM 选优。

当前示例合成 50,000 次采样得到条件命中率 `R1=0.601260、R2=0.526208、R3=0.097290`。最终平均结果如下：

| 指标 | 结果 |
|---|---:|
| 平均端到端延迟 | 13.036440 ns |
| 平均动态功耗（含 WB） | 97.519193 mW |
| 静态功耗 | 298.937209 mW |
| 刷新功耗 | 4.194304×10^-7 mW |
| 平均总功耗 | 396.456403 mW |
| FoM | 1.934842×10^-4 1/(ns·mW) |
| BER / endurance / refresh 约束 | 全部通过 |

三个可读的指令路径为：L3 命中的 load 为 `7.660 ns / 0.603552 nJ`；L2 命中的 store 为 `3.354 ns / 0.172776 nJ`；片外命中的 load 为 `51.610 ns / 11.075552 nJ`。完整逐层延迟、能耗、功耗、raw/effective 器件指标和约束位于 `results/scope_example.json`。

额外执行 `--explore` 后，3 个层级都实际遍历了 SRAM、SOT-MRAM、2D-eDRAM，共 27/27 个组合运行并通过约束；最高 FoM 映射为 `SRAM / SOT-MRAM / SOT-MRAM`，平均 `12.765098 ns / 216.892851 mW`，FoM 为 `3.611858×10^-4 1/(ns·mW)`。这只说明当前参数与合成 workload 下的结果，不构成固定层级映射；完整数据在 `results/scope_explore.json`。

本方案用于器件/容量/命中率的快速行为级比较，不声称替代电路级签核：截图未给 BER、OSFET BTI 寿命函数和 eDRAM pure-path 数值；DESTINY 也无法把数据阵列“单元漏电”和“外围漏电”进一步分开，因此当前严格替换整个数据阵列漏电并保留标签阵列漏电。这些缺口都以配置或报告字段显式暴露，后续拿到测量函数即可直接替换。
